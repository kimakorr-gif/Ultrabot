# 🚀 ULTRABOT - УСТАНОВКА НА ЛОКАЛЬНЫЙ ПК

**Просто и быстро в 3 шага!**

---

## ⚡ ВАРИАНТ 1: АВТОМАТИЧЕСКАЯ УСТАНОВКА (Рекомендуется)

### Linux / macOS
```bash
# Скопируй репозиторий
git clone https://github.com/kimakorr-gif/Ultrabot.git
cd Ultrabot

# Запусти автоматическую установку
chmod +x install.sh
./install.sh

# Всё готово! 🎉
```

### Windows
```bash
# Скопируй репозиторий
git clone https://github.com/kimakorr-gif/Ultrabot.git
cd Ultrabot

# Запусти установку
install.bat

# Всё готово! 🎉
```

---

## ⚙️ ШАГ 2: НАСТРОЙКА

```bash
# Скопируй пример конфига
cp .env.example .env

# Открой .env в редакторе и заполни:
nano .env  # или используй любой текстовый редактор
```

### Что нужно заполнить в `.env`:

```env
# Telegram Bot Token (получи от @BotFather)
TELEGRAM_TOKEN=ВАШ_ТОКЕН_СЮДА

# Telegram Канал (где публиковать новости)
TELEGRAM_CHANNEL_ID=-1001234567890

# Yandex Translate API Key
YANDEX_API_KEY=ВАШ_КЛЮЧ_СЮДА
YANDEX_FOLDER_ID=ВАШ_FOLDER_ID

# База данных (если используешь Docker - не трогай)
DATABASE_URL=postgresql://ultrabot:password@localhost/ultrabot

# Redis (если используешь Docker - не трогай)
REDIS_URL=redis://localhost:6379/0
```

---

## ▶️ ШАГ 3: ЗАПУСК

### Вариант А: С Docker (Рекомендуется)

```bash
# Запусти все сервисы
docker-compose up -d

# Запусти приложение
make run

# ИЛИ в режиме разработки с автоперезагрузкой:
make run-dev
```

### Вариант Б: Без Docker

```bash
# 1. Установи PostgreSQL
# macOS: brew install postgresql
# Ubuntu: sudo apt install postgresql
# Windows: скачай с https://www.postgresql.org/download/

# 2. Создай базу
createdb ultrabot
createuser ultrabot -P  # задай пароль

# 3. Запусти приложение
make run
# или
make run-dev  # с автоперезагрузкой
```

---

## ✅ ПРОВЕРКА РАБОТЫ

```bash
# Открой в браузере
http://localhost:8000/health

# Должно вывести:
{"status":"ok"}

# Остальные endpoints:
http://localhost:8000/metrics  # Метрики Prometheus
http://localhost:8000/stats    # Статистика приложения
http://localhost:8000/ready    # Готовность сервиса
```

---

## 🛠️ ПОЛЕЗНЫЕ КОМАНДЫ

```bash
# Запуск
make run              # Режим Production
make run-dev          # Режим Development с автоперезагрузкой

# Быстрый запуск (без make)
./run.sh              # Linux/macOS
run.bat               # Windows

# Тесты
make test             # Запустить все 39 тестов
make test-coverage    # С отчетом о покрытии

# Код
make lint             # Проверка качества кода
make format           # Автоформатирование

# Docker
make docker-run       # Запустить все сервисы
make docker-stop      # Остановить все
make docker-logs      # Посмотреть логи

# БД
make db-migrate       # Миграции БД

# Помощь
make help             # Все команды
```

---

## 📋 ТРЕБОВАНИЯ

### Минимум
- Python 3.11+
- PostgreSQL 14+ (или Docker)
- Redis 7+ (или Docker, или встроенный кэш)
- 2 GB RAM
- 500 MB свободного места

### Оптимально
- Python 3.12
- PostgreSQL 15+
- Redis 7+
- 4 GB RAM
- 5 GB свободного места
- Docker Desktop (для легкой установки)

---

## 🐍 ПРОВЕРКА PYTHON

```bash
# Проверь версию
python3 --version

# Должно быть 3.11 или выше:
# Python 3.11.7
# Python 3.12.1
```

**Если Python старше:**
- Скачай с https://www.python.org/downloads/
- Или используй пакетный менеджер:
  ```bash
  # macOS
  brew install python@3.12
  
  # Ubuntu
  sudo apt install python3.12 python3.12-venv
  ```

---

## 🤖 ПОЛУЧЕНИЕ TELEGRAM TOKEN

1. Открой Telegram
2. Найди **@BotFather**
3. Отправь `/start`
4. Отправь `/newbot`
5. Следуй инструкциям (дай боту имя)
6. Скопируй токен из ответа
7. Вставь в `.env`:
   ```env
   TELEGRAM_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
   ```

---

## 🗄️ YANDEX TRANSLATE API

1. Перейди на https://cloud.yandex.com/
2. Создай аккаунт или логинься
3. Создай новый проект
4. Перейди в "IAM" → "Service Accounts"
5. Создай сервис-аккаунт
6. Получи API ключ
7. Скопируй в `.env`:
   ```env
   YANDEX_API_KEY=ABC123...
   YANDEX_FOLDER_ID=your_folder_id
   ```

---

## ❓ РЕШЕНИЕ ПРОБЛЕМ

### "Python not found"
```bash
# Установи Python 3.11+
# https://www.python.org/downloads/

# Или через пакетный менеджер:
# macOS: brew install python@3.12
# Ubuntu: sudo apt install python3.12
```

### "Port 8000 already in use"
```bash
# Использовать другой порт:
python -m uvicorn src.main:app --port 8001

# Или убить процесс:
# Linux/macOS:
lsof -ti:8000 | xargs kill -9

# Windows:
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### "PostgreSQL connection refused"
```bash
# Запусти Docker:
docker-compose up -d postgres

# Или установи локально:
# macOS: brew install postgresql
# Ubuntu: sudo apt install postgresql
# Windows: скачай с postgresql.org
```

### "Redis connection refused"
```bash
# Запусти Docker (проще):
docker-compose up -d redis

# Или локально:
# macOS: brew install redis && redis-server
# Ubuntu: sudo apt install redis-server && redis-server
```

### "TELEGRAM_TOKEN not found"
```bash
# Создай .env файл:
cp .env.example .env

# Открой в редакторе и заполни значения
nano .env
```

---

## 🎯 ЧТО ДАЛЬШЕ?

### После запуска приложение:
- ✅ Работает на `http://localhost:8000`
- ✅ Собирает новости из RSS лент
- ✅ Публикует в Telegram канал
- ✅ Отслеживает метрики

### Мониторинг:
```bash
# Prometheus (метрики)
http://localhost:9090

# Grafana (дашборд)
http://localhost:3000
# Login: admin / admin

# Логи:
docker-compose logs -f
```

### Тесты:
```bash
# Запустить все тесты
make test

# С отчетом о покрытии
make test-coverage
# Открой htmlcov/index.html
```

---

## 📚 ДОКУМЕНТАЦИЯ

| Файл | Описание |
|------|---------|
| **[GETTING_STARTED.md](GETTING_STARTED.md)** | ⭐ Начни отсюда (5 минут) |
| **[INSTALL.md](INSTALL.md)** | Подробная установка |
| **[ARCHITECTURE.md](ARCHITECTURE.md)** | Архитектура системы |
| **[docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)** | Production развертывание |
| **[docs/MONITORING.md](docs/MONITORING.md)** | Мониторинг |
| **[docs/API.md](docs/API.md)** | API документация |
| **[docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)** | Разработка |

---

## 🎓 КРАТКОЕ ОПИСАНИЕ

Ultrabot - это production-ready приложение для агрегирования новостей из RSS лент с:

✨ **Функции**:
- 50+ источников новостей одновременно
- Умная фильтрация по ключевым словам (RPG, PS5, Xbox и т.д.)
- Оценка качества новостей
- Автоматический перевод на русский (сохраняет имена)
- Проверка дубликатов
- Публикация в Telegram с хэштегами

🛡️ **Надежность**:
- Автоматический retry при ошибках
- Circuit breaker для защиты от API сбоев
- Обработка ошибок на всех уровнях
- Graceful shutdown

📊 **Мониторинг**:
- Prometheus метрики (35+)
- Grafana дашборд
- Логирование всех операций

🔐 **Безопасность**:
- Все секреты в переменных окружения
- Никаких паролей в коде
- Non-root Docker контейнер

---

## ✅ ЧЕК-ЛИСТ УСТАНОВКИ

- [ ] Python 3.11+ установлен
- [ ] Репозиторий склонирован
- [ ] Запущен `./install.sh` (или `install.bat`)
- [ ] Создан и заполнен `.env` файл
- [ ] Docker сервисы запущены (`docker-compose up -d`)
- [ ] Приложение запущено (`make run`)
- [ ] Тесты проходят (`make test`)
- [ ] Здоровье приложения OK (`curl http://localhost:8000/health`)

---

## 🚀 ГОТОВО!

Приложение установлено и готово к использованию! 🎉

**Первые шаги:**
1. Проверь логи: `docker-compose logs -f`
2. Проверь здоровье: `http://localhost:8000/health`
3. Запусти тесты: `make test`
4. Читай документацию: [GETTING_STARTED.md](GETTING_STARTED.md)

**Вопросы?**
- Читай [INSTALL.md](INSTALL.md) для подробной помощи
- Проверь [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) для разработки
- Смотри [ARCHITECTURE.md](ARCHITECTURE.md) для понимания архитектуры

---

**Версия**: 1.0.0  
**Статус**: ✅ Production Ready  
**Последнее обновление**: Январь 2026
